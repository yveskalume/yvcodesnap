# App Styles

This folder is reserved for app-level style layers:

- `tokens.css` for design tokens and CSS variables.
- `globals.css` (optional) for global style composition.

Current global styles remain in `src/index.css`.
